import { Typography } from "@mui/material";

const CheckoutPage = () => {
  return (
    <Typography variant="h3">
      Only logged is users should be able to see this!
    </Typography>
  );
};

export default CheckoutPage;
